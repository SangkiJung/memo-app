spring:
  application:
    name: memo-app
  datasource:
    url: jdbc:p6spy:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:memoapp}
    username: ${DB_USER:memo}
    password: ${DB_PASSWORD:memo}
    driver-class-name: com.p6spy.engine.spy.P6SpyDriver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
    show-sql: false

server:
  port: 8080

memo:
  weather-api:
    api-key: ${WEATHER_API_KEY:}
    base-url: https://api.weatherapi.com/v1

logging:
  level:
    root: INFO
    com.example.memo: INFO
    org.hibernate.SQL: OFF
    org.hibernate.orm.jdbc.bind: OFF
    p6spy: INFO
